// src/composers/callback.ts
import { Composer } from 'grammy';
// import createDebug from 'debug';

// const debug = createDebug('bot:user:callback');

// 创建一个 Composer 实例
const callbackComposer = new Composer();

export default callbackComposer;

name: remote ssh command
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: executing remote ssh commands using password
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 107.191.52.157
          username: root
          password: )3zE_2VQe%Nks9M?
          port: 22
          script: cd /www/wwwroot/ordersystem-new.2024fc.xyz/task-backend && git pull --rebase origin main && npm run build && pm2 restart dist/index.js
      - name: Send Email
        uses: hilarion5/send-mail@v1
        with:
          smtp-server: smtp.gmail.com
          smtp-port: 465
          smtp-secure: true
          from-email: hfpp2012@gmail.com
          to-email: ganweiliang@swqztech.com
          username: hfpp2012@gmail.com
          password: ${{ secrets.SMTP_PASSWORD }}
          subject: task system github action
          body: 部署成功
          html: |
            <h1>github action</h1>
            <p>task system</p>